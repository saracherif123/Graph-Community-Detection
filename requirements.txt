# Requires Python 3.10–3.12 (gensim not yet compatible with 3.13)
# Setup: python3.12 -m venv venv && venv/bin/pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
# Core
numpy>=1.24.0,<2
networkx>=2.6.0
matplotlib>=3.4.0
seaborn>=0.11.0
pandas>=1.3.0
scipy>=1.9.0
scikit-learn>=1.0.0

# Shallow embeddings (Node2Vec, DeepWalk)
node2vec>=0.4.6

gensim>=4.0.0

# Optional: community detection
python-igraph>=0.9.0

# Jupyter
jupyter>=1.0.0

# PyTorch (CPU) - use: pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
torchvision>=0.15.0
torch-geometric>=2.4.0
