numpy>=1.21.0
networkx>=2.6.0
matplotlib>=3.4.0
seaborn>=0.11.0
python-igraph>=0.9.0
pandas>=1.3.0
jupyter>=1.0.0

# Recommended (optional) - PyTorch and PyTorch Geometric
# Install PyTorch for your platform first following https://pytorch.org/get-started/locally/
# Example CPU-only install:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# PyTorch Geometric has extra wheels; follow https://pytorch-geometric.readthedocs.io/en/latest/notes/installation.html
# Example (after installing torch):
# pip install torch-scatter torch-sparse torch-cluster torch-spline-conv torch-geometric

